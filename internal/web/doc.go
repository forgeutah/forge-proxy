// Package web embeds and serves the Forge Utah login page static assets
// via //go:embed. The shipped variant is the "card" layout from the
// React scaffold; other variants are deleted before embedding.
package web
