// Package auth implements the Slack OpenID Connect sign-in flow:
// /auth/login, /auth/callback, /auth/logout, the pre-auth state cookie,
// and the returnTo URL validator. Lives behind auth.forgeutah.tech.
package auth
